
$('#my-modal_filament').modal('hide');

       $('.field_filament').html('<%= j render "field_filament", filament: @filament %>');
        $('.pri').addClass("chosen-select");

         $('.chosen-select').chosen(
        {

          allow_single_deselect: true,
          no_results_text: 'No results matched',
          width: '250px'
        });